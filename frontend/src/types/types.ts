export interface User {
  name: string;
  email: string;
  photo: string;
  gender: string;
  role: string;
  _id: string;
  dob: string;
}

export interface Product {
  name: string;
  photo: string;
  price: number;
  stock: number;
  category: string;
  _id: string;
}
